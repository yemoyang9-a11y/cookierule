{
  "name": "cookierule",
  "version": "0.1.0",
  "private": true,
  "description": "Cookierule - view, edit, protect, save and restore cookies (browser extension, Manifest V3)",
  "type": "module",
  "scripts": {
    "icons": "python3 scripts/make_icons.py",
    "test": "node --test test/*.test.mjs",
    "test:e2e": "node test/smoke.mjs",
    "build": "node scripts/build.mjs",
    "lint:network": "node scripts/check_network.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.0"
  }
}
